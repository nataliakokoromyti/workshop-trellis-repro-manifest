cvm-version: 0.7.1
cpus: 16
memory: 65536
gpus: 8

containers:
  - name: api
    image: python:3.11-slim
    runtime: nvidia
    gpus: all
    ipc: host
    command:
      - python
      - -m
      - http.server
      - "8080"

shim:
  upstream-port: 8080
  paths:
    - /
